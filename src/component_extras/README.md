Crate which contains common properties which an array of different crates might want to interact with, but without the need to link with those particular crates.

E.G:

['Selected']
['Followed']

etc...